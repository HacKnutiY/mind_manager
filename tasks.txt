Итого:
1. [Теория] async в Dart. Собрать уже написанную инфу, убрать лишнее, структурировать
2. [Практика] номер 1 в списке

Режим работы: начиная с первого экрана все доводим до ума. То есть грубо говоря 
на каждом экране производим фичи из списка и рефачим то что к нему относится. А то так 
не начну работу в принципе
архитектуру и солид паралелльно рассматриваем


фичи:
0) навигация. проайдер расположенный высоко по дереву требует, 
чтобы переход с activities на activity происходил через  
Navigator.push(
  context, // тот же контекст, который "видит" Provider
  MaterialPageRoute(builder: (context) => Activity()),
);
1) 2 списка задач - актуальные и неактуальные
[ключи, provider]. Ключи пригодились бы в случае, если
бы мы раюотали с порядком элементов в пределах одного списка.
тут же мы просто между списками перекидываемся

2) смена темной и светлой темы
[темы]
3) адаптив(не важно какой именно), верстка + тестирование виджетов
[верстка + адаптив]
4) поиск тасков. подключаем тут провайдер
[провайдер]
5) валидация полей (+)
[ключи]
6) работа с датами. спринт завершен - дисактивить его
[]
---------

1) Работа с датами.
- DatePicker и форматирование дат
- 


n) тестирование: виджетов + юнит тесты попробовать
задачи:

---------
рефакторинг на сейчас: 
0. фильтрация долгосроков в activities и activity прямо в билдере прописана.
ее в модель надо + работу с боксами в сервис


рефакторинг на потом:
общие: 
0. formatDate(intl)

1. Сохранять состояние тасков/целей при выходе из приложения(+)
попробовать сохранять, но теперь при dispose модели. кажется так будет оптимизированнее 
чем каждый раз сейвить состояние тасков


Активности:
1. на экран активности и создания компонентов активности прописать имя 
направления в аппбаре

2. верстку на экране исправить. все место - заметкам, а удаление к низу прибито чтобы было

Спринты:

5. Добавить категорию Прочее при выборе направления

6. Добавить описание на экране спринта

7. (когда есть активный спринт, значок таба с зеленым кружком в углу)


Долгосроки:
1. На виджете долгосрока направление тоже отмечать

2. завершенные долгосроки на экране направления в конец закидывать

3.  со списком внутри как я понял должен быть в модели, тк свзяан с состоянием.
Но у меня он в сервисе (term_goal_service) + статичный, что мне кажется не оч хорошо
Но в сервисе он не просто так - он нужен в 2 моделях сразу - term_goal_tile_model и activities_model

4. поменять виджет долгосрока. Срок до: xx/xx/xx(осталось N дней/цель просрочена)(+)

5. форматирование полей, чтобы долгосрок был сроком не менее 30 дней



Таски:
1. обрезки двух категорий(акт/неакт) исправить
2. как то раполовиниить 2 выпадающих списка. напр через expanded
3. Добавить категорию Прочее при выборе направления
4. Добавить приоритетность




1. названия методов/полей и тд в одном стиле
-6. везде где прописана дата, поправить через formatDate(как в спринте), а не вручную
3. newTask и newTaskScreen. абстарктный класс и его потомок. разобраться, 
все ли там адекватно выглядит
4. спринтТаск - > спринтГоал
5. в утилиты вынести часто используемые компоненты:
- виджеты
  долгосрочка
  таска(возможно, разделим обычную и спринтовую)
  поля
  кнопки добавления/удаления
- методы
  валидация полей
  валидация DateTime для нужного отображения в полях
  генерация id
6. правильно организовать работу с боксами. не знаю как именно пока
ну как пример: закрытие бокса при закрытии модели
8. некоторую работу view модели взял на себя вью. Например на
экране NewTaskScreen
9. рассмотреть шаг за шагом как происходит удаление/добавление таска(спринта). 
можно найти много странностей
10. состояние task не сохраняется при выходе из приложения, тк загрузка данных происходит 
только во время создания, а обновления и загрузка последнего состояния в бокс данных нет впринципе
11. изменить:
- имя бокса для таксов спринта -> SprintGoals, чтобы без путаницы
- ..
12. сам класс task тоже. а точнее сепарировать 2 класса - task и sprintGoal, тк со 
временем все равно стало больше различий
13. для смены категории задачи не долгое нажатие, а три 
точки -> актив/дисактив


--------
баги:

--------
вопросы:
1. вспомнить как я исправил проблему  удалением через свайп задачи(и чтобы 
дерево не жаловалось)
2. почему состояние с асинхрокой не дружит? setState не может быть async
3. как состояние долгосрока сохраняется без term.save() и при перезапуске экрана завершенные задачи
не отображаются больше
----------
памятка:
  если какая то вью только на одном экране встречается, то можно 
  ей назначить модель экрана на которой она находится. очевидно, но да
  прописываем поля/методы в МОДЕЛИ если работаем с данными
  состояния. Например: список целей, который мы вскармливаем билдеру

  прописываем поля/методы в СЕРВИСЕ если данные не относятся к состоянию
  - Функция работает с персистентными данными (Hive, API)
  - Функция содержит бизнес-логику (валидация, преобразования)
  - Функция может быть полезна в нескольких несвязанных модулях

  виджет взаимодействует только с моделью

теперь работаем в таком формате. параллельно работаем с моделью
цели например и экраном где эта цель используется - activities
1. прописаываем в модели методы-сценарии
2. дробим эти сценарии на подфункции
3. разбираем: если функция работает не с данными состояния и 
часто будет исполььзоваться и на других экранах - перекидываем в сервис  

изменено:
по подходу llf
  provider - обращения к сети/лок базам данных
  service - вся бизнесс логика и взаимодействие с провайдерами(сетью/бд)
  viewmodel - тоже логика(посредством обращения к сервису и слизывания уже готовых методов) 
  и данные, преобразованные в подходящий для view вид
  view - максимально тупой компонент, задача которого - отображать данные 
  которые передал viewmodel
  --
  отдельную model для view создаем(придется создать) в случае, когда view 
  на нескольких экранах используется



доработал задумку mind_manager, и он стал сложнее.
что поменялось:
1) Вид виджета спринта. Теперь это - выпадающий список среднесрочных целей
2) Виджет цели спринта - среднесрочные цели. Теперь, сущности в выпадающем списке - карточка
Цель можно выполнить и по ней перейти на экран создания задачи
3) Виджет таска. Теперь таск тоже выпадающий виджет, в нем уже могут храниться подтаски
и таски и подтаски - чекбоксы
экран создания спринта не меняется


- через спринт можно перейти на экран спринта, на нем будем 
удалять/добавлять таски. а выполнение будет фиксироваться за счет 
чебокса(виджет как и был)







------------
ЗЕМЕТКИ И ВОПРОСЫ

поговорим о инхеритед нотифаер
1. Объявить инхерит. Передать ему в родительский конструктор параметр нотифаер типа changeNotifier
2. Сделать его родительским для виджетов, по которым будем проносить данные
3. Обращаемся к модели в инхерите посредством методов контекста по поиску по дереву. Можно подписаться с 
подпиской и без. поиск без подписки полезен в случае когда из модели нужны константы или какие то методы модели;
с подпиской в случае когда за изменениями данными в моделе нужно следить и менять
4. НотифайЛистенерс. метод перерисовывает только подписанные на него виджеты

почему же даже при использовании инхеритед нотифаер использовали фул виджет
наша задача в чем? - сохранить состояние объявленной модели(changeNotifier), чтобы ее поля не обнулились
прописать переменную модели в билде заведомо странное решение и это очевидно, даже разраб не знает сколько 
раз перевызвоится билд. 
далее, что насчет того чтобы прописать его в поле лесс виджета? в таком случае виджет перестроиться вместе с нашей 
моделью в случае если перестроиться родительский виджет .
ну тогда и остается фул виджет. потому что мы прописываем это поле не в самом виджете, а в его состоянии. 
То есть это поле наша модель уже будет полем состояния. А состоние строится только один раз - при initState()   


где прописывать получение аргументов с другого экрана - didChangeDependencies. ModalRoute - инхерит потому что
но не всегда. можно и в инитСтейт, но тогда уже передача через конструктор проводится

------------

